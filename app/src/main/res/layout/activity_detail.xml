<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Destination Image -->
        <ImageView
            android:id="@+id/detail_image_view"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop" />

        <!-- Destination Name -->
        <TextView
            android:id="@+id/detail_name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Name" />


        <!-- Destination Description -->
        <TextView
            android:id="@+id/detail_description_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Description of the destination."
            android:layout_marginTop="8dp" />

        <!-- Best Locations -->
        <TextView
            android:id="@+id/detail_best_locations_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Best locations to visit nearby."
            android:layout_marginTop="16dp" />
    </LinearLayout>
</ScrollView>
